{
  "version": 2,
  "buildCommand": "cd NivHenn_OnlyUI && npm install && npm run build",
  "outputDirectory": "NivHenn_OnlyUI/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
